<form method="post" action="{{ path('app_scandoc_delete', {'id': scandoc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scandoc.id) }}">
    <button class="btn">Delete</button>
</form>
