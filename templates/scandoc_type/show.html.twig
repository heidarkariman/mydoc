{% extends 'base.html.twig' %}

{% block title %}ScandocType{% endblock %}

{% block body %}
    <h1>ScandocType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scandoc_type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ scandoc_type.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scandoc_type_index') }}">back to list</a>

    <a href="{{ path('app_scandoc_type_edit', {'id': scandoc_type.id}) }}">edit</a>

    {{ include('scandoc_type/_delete_form.html.twig') }}
{% endblock %}
